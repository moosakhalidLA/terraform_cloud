import "tfplan/v2" as tfplan

print("Ensure that instance type is t2.micro")

instance_type_rule = rule { tfplan.resources.aws_instance.example[0].diff["instance_type"].new is "t2.micro" }

main = rule {
	instance_type_rule
}
